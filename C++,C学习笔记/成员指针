是C++中用于对类中成员进行操作。
成员指针的定义格式：成员类型 类名::*指针名=&类名::成员名;
成员函数指针的定义格式: 成员函数返回类型 类名::*指针名 = &类名::成员函数名（参数表）;
例：class A
{
int m;
public:
void func(){};
...
};
定义成员指针： int A::*p = &A::m;
定义成员函数指针： void (A::*f)() = &A::func;
